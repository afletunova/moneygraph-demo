"""No settings endpoints exist yet. Scaffolded router so this domain has a
home when one is needed, matching the nodes/candidates/pipeline/graph split.
"""

from fastapi import APIRouter

router = APIRouter()
